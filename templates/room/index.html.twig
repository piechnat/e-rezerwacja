{% extends 'base.html.twig' %}

{% block title %}{{ 'Indeks sali'|trans }}{% endblock %}

{% block main %}
  <h2 class="main-title">{{ 'Indeks sali'|trans }}</h2>
  <ul>
  {% for room in rooms %}
    <li>
      <a href="{{ path('room_show', {'id': room.id}) }}">{{ room.title }}</a>
    </li>
  {% endfor %}
  </ul>
{% endblock %}