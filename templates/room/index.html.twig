{% extends 'base.html.twig' %}

{% block title %}{{ 'Lista sal'|trans }}{% endblock %}

{% block main %}
  <h2>{{ 'Lista sal'|trans }}</h2>
  <ul>
  {% for room in rooms %}
    <li>
      <a href="{{ path('room_edit', {'id': room.id}) }}">{{ room.title }}</a> →
      [ <a href="{{ path('reservation_view_week', {'id': room.id}) }}">rezerwacje</a> ]
    </li>
  {% endfor %}
  </ul>
{% endblock %}