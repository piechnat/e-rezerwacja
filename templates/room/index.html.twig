{% extends 'base.html.twig' %}
{% set main_title = 'Indeks sal' %}
{% block main %}

  <h2 class="main-title">{{ main_title|trans }}</h2>
  <ul>
  {% for room in rooms %}
    <li>
      <a href="{{ path('room_show', {'id': room.id}) }}">{{ room.title }}</a>
    </li>
  {% endfor %}
  </ul>

{% endblock %}