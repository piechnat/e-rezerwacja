{% extends 'base.html.twig' %}

{% block title %}{{ 'Edycja użytkownika'|trans }}{% endblock %}

{% block main %}
  <h2>{{ 'Edycja użytkownika'|trans }}</h2>
  {{ form_start(form) }}
    {{ form_widget(form) }}
    <div class="form-group button-row">
      <button type="submit" name="save" value="save">
        <i class="fas fa-save"></i>{{ 'Zapisz'|trans }}
      </button>
    </div>
  {{ form_end(form) }}
  {{ include('jqslct2-load.html.twig') }}
{% endblock %}