{% extends 'base.html.twig' %}

{% block title %}Informacje i pomoc{% endblock %}

{% block main %}
<div>
  <h2 class="main-title">{{ 'bx bx-info-circle'|ico }}Informacje i pomoc</h2>
    <p>
      E-rezerwacja to system informatyczny w formie aplikacji internetowej stworzony dla pracowników
      i&nbsp;studentów Akademii Muzycznej w Łodzi w celu umożliwienia zdalnej rezerwacji sal oraz 
      usprawnienia ewidencji pobrań i zwrotów kluczy.
    </p>

  {% if app.request.locale == 'pl' %}

    {% if not is_granted(constant('App\\CustomTypes\\UserLevel::USER')) %}
      <p><a href="{{ path('login') }}">Zaloguj się przez pocztę <b>amuz.lodz.pl</b></a></p>
    {% endif %}

  {% else %}

    {% if not is_granted(constant('App\\CustomTypes\\UserLevel::USER')) %}
      <p><a href="{{ path('login') }}">Log in via the mail <b>amuz.lodz.pl</b></a></p>
    {% endif %}

  {% endif %}
  
    <p>
      Pytania i uwagi odnośnie aplikacji oraz zgłoszenia ewentualnych błędów można kierować na adres:
      <a href="#" class="cryptedmail" data-name="mateusz.piechnat" data-domain="amuz.lodz" data-tld="pl"
        onclick="window.location.href = 'mailto:' + this.dataset.name + '@' + this.dataset.domain + '.' + this.dataset.tld + '?subject=e-rezerwacja'; return false;"></a>
    </p>
{% endblock %}